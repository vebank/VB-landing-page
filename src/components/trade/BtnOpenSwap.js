import React from 'react';

const BtnOpenSwap = ({...props}) => {
    return (
        <button
            {...props}
        >
            Swap
        </button>
    );
};

export default BtnOpenSwap;
